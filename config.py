

postgresql_hosts = {
    'local': 'localhost',
    'docker': 'postgresdb'
}

postgresql_host = postgresql_hosts['docker']

redis_hosts = {
    'local': 'localhost',
    'docker': 'redis'
}

redis_host = redis_hosts['docker']
